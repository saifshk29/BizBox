import React from 'react'
import '../Components/loading.css'

const Loading = () => {
  return (
    <div className="loader text-white h-screen w-screen flex items-center justify-center">Loading</div>
  )
}

export default Loading